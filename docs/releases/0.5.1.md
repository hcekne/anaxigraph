---
version: "0.5.1"
publication: published
publication_evidence: https://pypi.org/project/anaxigraph/0.5.1/
artifacts: verified
artifacts_evidence: https://github.com/hcekne/anaxigraph/releases/tag/v0.5.1
protected_merge: bypassed
protected_merge_evidence: https://github.com/hcekne/anaxigraph/pull/7#issuecomment-5618055041
production: verified
production_evidence: https://github.com/hcekne/anaxigraph/issues/6
ledger: docs/releases/0.5.1.md
---

# AnaxiGraph 0.5.1

Published and deployed on 10 September 2026 through the trusted release workflows.
A fresh public installation reports 0.5.1. The retained production index passed
upgrade, exact retained-data parity, live interfaces, a real model-backed semantic
write, and post-write full health. AI-map and architecture-review completion remain
separate checks below, not consequences of successful deployment.

## Corrective scope

- [x] Remove the obsolete `file_versions` foreign keys from all three legacy
  semantic tables before compatibility compaction. Preserve canonical file facts,
  document identities, other constraints, and indexes inside the existing migration
  transaction. Refuse retirement when a module record lacks its canonical fact.
- [x] Repair the same obsolete constraints when opening an already-upgraded
  schema-11 index affected by 0.5.0. Skip that repair transaction on unaffected opens.
- [x] Detect missing foreign-key parent tables in migration validation and index
  health, including empty tables and NULL references that `foreign_key_check` misses.
- [x] Explicitly close backup and initialization connections. Produce standalone
  rollback-journal backups, restore closed legacy WAL backups from read-only storage,
  and reject a backup with a nonempty sidecar journal instead of silently ignoring it.
- [x] Install commit, pre-merge, and push hooks. Run the documented tests and coverage
  gates before push, check release/ledger/version consistency, compare publication
  with PyPI, and reject newly introduced merge history in CI. Local hooks do not
  replace server-side review or administrator enforcement.
- [x] Correct the 0.5.0 publication and failed-rollout record without pretending
  its approving-review and linear-history protections were satisfied.
- [x] Rehearse upgrade, restore, retained-data parity, and semantic writes against
  both the actual schema-10 backup and the failed schema-11 rollout backup.
- [x] Pass the full quality, browser, package, and generated-container PR checks.
  All seven required CI checks passed; the independent-review exception is below.
- [ ] After a verified production upgrade, refresh changed semantics with GPT-6
  Astra/medium and finish the outstanding architecture review with GPT-6 Astra/max.
  Keep unchanged-scope reuse and snapshot provenance; a running worker is unfinished.

## Release and deployment ledger

- [ ] <!-- release:protected_merge --> Independent review was **bypassed**, not
  fulfilled, under the owner's explicit 10 September authorization. PR #7 used a
  single-parent squash merge; linear history was preserved and protections unchanged.
- [x] <!-- release:publication --> Publish immutable `v0.5.1` through the existing
  trusted PyPI workflow and verify a fresh public installation.
- [x] <!-- release:artifacts --> Verify wheel, source archive, plugin ZIP, checksums,
  SBOM, attestations, reproducibility, and the matching multi-architecture image.
- [x] <!-- release:production --> Back up the live retained index, preserve both
  repositories and hardened deployment settings, deploy the verified digest, and
  verify real semantic writes as well as REST/MCP/dashboard and full index health.

## Compatibility and evidence

This patch keeps schema version 11. Upgrades from the supported released schemas
still use a validated automatic backup and atomic migration. An index already
affected by 0.5.0 receives a targeted transactional constraint repair; it does not
need its immutable facts or AI descriptions regenerated. Back up before cutover,
and retain the matching old image and index for rollback.

The regression fixture reproduces the released inline foreign-key declarations,
not merely a fresh schema with its version number changed. Both schema-10 upgrade
and schema-11 repair now exercise saved-document reuse, scope upserts, job claiming,
submission, and document insertion after initialization. Dedicated recovery tests
retain connection references to prevent garbage collection from hiding leaks and
exercise read-only legacy backups and nonempty-journal refusal.

On 8 September, **36 focused migration/recovery/checkpoint tests** passed, as did
**42 combined hook, legacy-schema, and recovery regressions**. Whole-repository
lint, format, size, and architecture checks passed. All seven required
[PR CI checks](https://github.com/hcekne/anaxigraph/actions/runs/34244154399) passed.

The first real pre-push run completed **990 of 991 tests**, with **93.43% total
coverage** and **100% changed-line coverage (47/47)**. It correctly blocked the push:
one hook test inherited the outer push's branch variables instead of isolating its
temporary repository. After isolating those variables, the complete real push gate
passed **all 991 tests**, with the same coverage totals, on commit
`4731c81f56804bec5bf7edd74f84f20419878887`. No hook or runtime check was skipped.

The clean candidate also passed **all 40 dashboard browser contracts**, both
Compose validations, the bounded benchmark, and three first-user journeys (median
dashboard **1.539 s**, first dossier **1.695 s**). Its wheel and source archive
validated and installed independently as 0.5.1; its matched agent plugin ZIP built.
The generated sidecar smoke verified parser loading, MCP, and every hardening check.
These are candidate checks, not publication or production acceptance.

The immutable tag `v0.5.1` names the single-parent squash commit
`ab8491dff04b7c3dc9ba36ca4976f3667f42d432`; its tracked tree is identical to the
CI-tested PR head. The [trusted release workflow](https://github.com/hcekne/anaxigraph/actions/runs/34472616638)
built and attested the bundle, published to PyPI, attached durable GitHub assets,
and passed its fresh public-install check. An independent cache-refreshed public
installation also reports 0.5.1. The wheel, source archive, and plugin rebuilt
byte-for-byte identically with CI's `umask 022`; all seven bundle attestations
verified against the tagged workflow and exact commit. Verified SHA-256 values:

- Wheel: `20582d59010da4aa069f0fc46aeb9d2f6029bb0d95fdf7fc24cd516a8f43abf0`.
- Source archive: `4b5a9efc4047bf8c31b02631acb6088fa998b09d0692c8311b8cc2500ca937b1`.
- Agent plugin: `59d6e12408baca4784ad0e25be35306366730d645c8838f70fad709ee0ba62d2`.

The matching [container workflow](https://github.com/hcekne/anaxigraph/actions/runs/34472441249)
and all seven [main CI checks](https://github.com/hcekne/anaxigraph/actions/runs/34472361484)
passed. Both Linux amd64 and arm64 images have SLSA provenance and SPDX SBOMs;
the registry attestation verifies the exact tag and source commit. Immutable image:
`ghcr.io/hcekne/anaxigraph@sha256:9ef7c5a491caaa19892cf32ade5074597a0daba589a00482e751c0ff0e99dccd`.
The published image passed an isolated generated-container smoke with parser loading,
MCP access, and every hardening check. A fresh public-package first-user journey
also passed, including a test semantic submission confined to its disposable index.

### Verified production cutover — 10 September

Only the AnaxiGraph service was paused, from **12:05:32 to 12:07:33 UTC**. A fresh
validated schema-10 backup retained **1,753,067,520 bytes**, SHA-256
`ec75740c580c8fbe4e5869523cae769998ee19ed7eb156aae70dc83af7a52c64`.
The official image upgraded the existing named volume using the normal backup and
initialization APIs, without custom SQL repair. Migration took **34.186 s**;
migration plus parity and full-health verification took **54.859 s**.

Exact before/after counts matched: **2 repositories, 1,200 snapshots, 144,937 file
facts, 7,682 semantic documents, 6,105 semantic claims, and 566,589 coverage rows**.
Every pre-existing non-retired saved-document field value was unchanged; six sampled
historical/current frames retained identical file, symbol, and relationship digests.
Both repository identities, the retained named volume, all bind mounts, and the
read-only root, dropped capabilities, no-new-privileges, tmpfs, restart policy, and
loopback binding were preserved. Resolved Compose changed only by the image pin.

Live REST, dashboard, and normal/executor MCP passed on 0.5.1 with both repositories.
A bounded **GPT-6 Astra/medium** run completed real production job **27154**, saving
document **7683** on snapshot **1202** at **12:13:11 UTC**, with no failed jobs.
Post-write full health passed: schema 11, integrity OK, zero foreign-key violations,
no missing parent tables, exact canonical semantic references, no lineage/parity
issues, and at most 15 reconstructed deltas. Its token usage was not reported by the
executor and is recorded as unknown, not as a free call. This write proves the
deployed regression is corrected; it does not complete the repository-wide AI map.
The fresh backup and matching old image remain available for rollback.

Two private retained-index rehearsals passed restoration, canonical read parity,
semantic planning/reuse, job claiming, a synthetic test submission, and post-write
health. Synthetic probe descriptions stayed in disposable copies, never production:

- The schema-10 copy retained 2 repositories, 1,132 snapshots, 144,288 file facts,
  7,682 semantic documents, 6,105 semantic claims, and 540,337 coverage rows before
  the deliberate write probe. Restore took **21.259 s**, migration **30.006 s**,
  and the full rehearsal **103.802 s**.
- The failed-rollout schema-11 copy retained 2 repositories, 1,156 snapshots,
  146,351 file facts, 7,682 semantic documents, 7,041 semantic claims, and 540,922
  coverage rows before its probe. Restore took **20.381 s**, same-version repair
  **2.877 s**, and the full rehearsal **76.740 s**.
- Six sampled historical/current frames per copy had identical file, symbol, and
  relationship digests before and after repair. Both repositories' identities and
  current snapshots were unchanged. Full health passed with no missing FK parents
  or row violations and at most 15 traversed deltas. This is upgrade/write evidence,
  not a completed AI map or a claim that the production deployment has occurred.

A third rehearsal restored the actual schema-10 backup from a read-only single-file
mount inside the candidate container, with a read-only root, dropped capabilities,
and no-new-privileges. Restore plus automatic upgrade completed in **96.314 s**,
reported schema 11 healthy, and created no sidecars beside the backup. Peak state
usage was **3,650,050,296 bytes**, including its automatic backup; peak WAL size was
**231,020,792 bytes**. A separate loopback-only staging service then passed REST,
dashboard, and normal/executor MCP checks with both retained repositories and
snapshot 1132 preserved. Its isolated container was stopped after verification;
production and the original backups were untouched.

The owner requested this corrective release on 8 September 2026. Its measured
production-source cost is **61 lines (0.10%)**, moving the exact Phase 13 ratchet
from **63,524 to 63,585 lines**. This is the measured incident correction, not a
general growth allowance. It reuses existing migration, integrity, and recovery
modules: no runtime dependency, new table, command family, or new size/complexity
exception. The 500-line module and 50-line/15-complexity function limits are unchanged.

The [0.5.0 ledger](../architecture-intelligence-0.5.0.md) retains the failed rollout
and rollback evidence. [Incident #6](https://github.com/hcekne/anaxigraph/issues/6)
was resolved after the corrective production write and post-write health passed. Next
release-schedule checkpoint: **15 September 2026**; review remaining regressions
and dependencies and record the decision without publishing an unready build.
