---
version: "0.5.0"
publication: published
publication_evidence: https://pypi.org/project/anaxigraph/0.5.0/
artifacts: verified
artifacts_evidence: https://github.com/hcekne/anaxigraph/actions/runs/34229098332
protected_merge: bypassed
protected_merge_evidence: https://github.com/hcekne/anaxigraph/pull/5
production: rolled_back
production_evidence: https://github.com/hcekne/anaxigraph/issues/6
ledger: docs/architecture-intelligence-0.5.0.md
---

# AnaxiGraph 0.5.0

Published on 8 September 2026. **Do not upgrade an existing index to 0.5.0.**
Production semantic writes failed after migration and production was rolled back
to its healthy 0.4.0 image and validated pre-upgrade index. PyPI 0.5.0 remains
published and is not yanked; the GitHub release is marked prerelease with an upgrade
warning. A corrective release is required; see [incident #6](https://github.com/hcekne/anaxigraph/issues/6).

## Highlights

- Oversized Codex inputs use lossless temporary evidence pages, avoiding the CLI
  character limit. Fresh-eyes packets also select representative modules and
  boundary links and enforce an explicit serialized evidence budget.
- Failure modes have distinct score meanings and a `remediate` recommendation.
  AnaxiGraph no longer encourages adopting or strengthening a harmful structure;
  unsafe legacy advice is withheld from actionable views.
- Incremental status explains saved-document reuse. A prior Living Charter remains
  available as explicitly stale context while changed evidence is refreshed.
- Parent-scope pattern checks receive bounded child contracts, responsibility
  witnesses, boundary links, and source excerpts. The existing Work Queue card
  recognizes durable-queue and lease evidence without expanding the catalogue.
- Review goals persist through resume and reach the comparison and final review.
  A goal change reuses blind proposals and adjudication; raw goals are withheld from
  those stages because they can contain implementation details.
- A small model-backed judgment benchmark distinguishes justified existing designs,
  harmful structures, unnecessary changes, and genuinely insufficient evidence.

This release also includes the post-0.4.0 multi-model review work: selectable
generations and comparison, grounding checks, executor diversity and pins, declared
context, accurate usage provenance, guarded writes, and clearer operational errors.

## Compatibility and scope

Upgrading from the published 0.4.0 release may migrate the index to schema 11.
The 0.5.0 migration leaves legacy foreign-key declarations pointing to the removed
`file_versions` table in three semantic tables. Reads and row-level integrity
checks passed, but subsequent semantic writes failed. The earlier compatibility
verification did not cover this legacy-schema write path. Back up the index before
any future corrected upgrade.
Rollback must use the matching pre-upgrade index backup, not assume an older binary
can read a newer schema.

Missing relationship-set and reference lookup indexes are installed before upgrade
compaction and restored when reopening an existing index. This avoids repeatedly
scanning large retained histories. Duplicate cleanup explicitly preserves coverage
and snapshot references while migration disables cascade actions, and refreshes
checkpoint fingerprints. Backup and migration validation remain mandatory.

The derived checkpoint policy also bounds actual ancestry in forked histories,
not only global snapshot sequence numbers. Existing caches are rebuilt once under
`bounded-delta-v4`; immutable facts and AI descriptions are retained.

`pattern-scores-v2` changes failure-mode interpretation and adds `remediate` to
recommendation filters. The explanation contract is `pattern-explanation-v3`.
Saved assessments are not rewritten: pattern work is revalidated under the new
contract. File-reading and contextual-description contracts are unchanged, so this
does not require rereading every unchanged source file.

`fresh-eyes --start --goal "…"` and the existing REST/MCP/dashboard workflow accept
an optional goal of at most 4,000 characters. Omission preserves the saved goal;
an explicit empty goal clears it. Evidence selection and compaction are disclosed
in saved manifests. Missing sampled evidence is never proof that code should be
deleted or that a pattern is absent.

No new runtime dependency, index table, public command family, normal MCP tool,
review stage, or automatically applied refactor is introduced.

## Verification

The [feature and release ledger](../architecture-intelligence-0.5.0.md) records
publication, artifact identities, the failed deployment and rollback, and the
unfinished semantic work. Publication is not successful production acceptance.

PR #5 merged with an owner-authorized administrator override: zero approving
reviews and a merge commit despite the required linear history. Neither protection
was satisfied; authorization was not an approving review. The immutable tag is
`v0.5.0` at `9bd115c62c434809808aecbd40b8c7c8e0e3b8e2`. The trusted
[publication workflow](https://github.com/hcekne/anaxigraph/actions/runs/34229098332)
and [container workflow](https://github.com/hcekne/anaxigraph/actions/runs/34229014677)
succeeded. The release is not production-complete.
