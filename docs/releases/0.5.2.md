---
version: "0.5.2"
publication: published
publication_evidence: https://pypi.org/project/anaxigraph/0.5.2/
artifacts: verified
artifacts_evidence: https://pypi.org/project/anaxigraph/0.5.2/#files
protected_merge: pending
production: pending
ledger: docs/releases/0.5.2.md
---

# AnaxiGraph 0.5.2

Published to PyPI on 10 September 2026 at 13:37 UTC. The public description now
contains the absolute logo URL, and both public distribution hashes match the
inspected local artifacts. The image URL used by the rendered PyPI page returns
HTTP 200, `image/svg+xml`, and the existing 1,171-byte logo through PyPI's image
proxy. This is a PyPI-only release, not a service deployment.

PyPI README-logo correction only. Replace the repository-relative image source
with an absolute HTTPS URL pinned to the existing, unchanged logo. Synchronize
the package, lockfile, and plugin version metadata; no runtime code, dependencies,
database schema, other README links, or semantic workflows change.

The owner explicitly requested this publication on 10 September 2026 without
running other tests. Local test hooks, CI suites, container builds, and semantic
scans are intentionally skipped for this release, not represented as passing.
Used the existing configured PyPI upload credential for this one-off packaging
correction; the normal trusted-publisher workflow and repository protections are
not modified. Checks are limited to the logo URL and the distribution metadata,
contents, checksums, and resulting public PyPI description.

## Release and deployment ledger

- [ ] <!-- release:protected_merge --> Source integration and review remain pending
  in [PR #9](https://github.com/hcekne/anaxigraph/pull/9); no protected merge or
  independent approval is claimed. CI was intentionally skipped, not left running.
- [x] <!-- release:publication --> Published the immutable 0.5.2 wheel and source
  archive and verified the public PyPI version, hashes, and corrected logo source.
- [x] <!-- release:artifacts --> Inspected the wheel and source archive. All 316
  packaged runtime files are byte-identical to 0.5.1; dependencies are unchanged.
  No full-suite, container, reproducibility, or attestation verification is claimed.
- [ ] <!-- release:production --> No service deployment is requested or performed.
  The production container remains on the verified 0.5.1 image.

## Exact artifacts

The immutable `v0.5.2` tag names `fb7f23f743a4a3ed53fb4527a43f23b3da70adc7`.
The tag retains its pre-publication record; this follow-up records actual publication
without modifying that tag or either uploaded archive.

- Wheel: `82c9dc003eb2b9dee541b2afeaa35ca2d747535c048a2abdc886c0ed03367597`.
- Source archive: `421d7ece02704bc074e9d2d30b19eb989ce0f779f5cbf7d60adca06e9ed9955d`.

No GitHub Release event was published, because it would start the normal release
workflow and its additional runtime checks. The source tag and pull request are
public; PyPI is the published distribution for this deliberately limited release.
